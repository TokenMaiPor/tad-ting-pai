## What & why

<!-- One or two sentences. Also add a plain-language entry to docs/maintainer-log.md. -->

## Checklist

- [ ] `npm run lint`, `npm run typecheck` and `npm test` pass
- [ ] New or changed rules have `examples` **and** `keep` cases
- [ ] No network calls added (`npm run build` then `npm run audit:network`)
- [ ] `docs/maintainer-log.md` updated
